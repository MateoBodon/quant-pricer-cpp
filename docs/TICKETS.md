# Tickets

Tickets are stored under `docs/tickets/`.

- Use tickets for scoped, reviewable work.
- Each ticket should produce at least one run log under `docs/agent_runs/`.

See also:
- `docs/CODEX_SPRINT_TICKETS.md` (queue)
- `docs/DOCS_AND_LOGGING_SYSTEM.md` (logging contract)
