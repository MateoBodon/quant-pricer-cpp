Tracked agent run logs (small files only). Use: YYYYMMDD_HHMMSS_TICKET-####_slug/
Include PROMPT.md, RESULTS.md, TESTS.md, META.json.

Note: GPT bundles are emitted to `artifacts/_local/gpt_bundles/` (ignored). Bundling is allowed on dirty trees; the tool stashes temporarily or bundles from HEAD.
