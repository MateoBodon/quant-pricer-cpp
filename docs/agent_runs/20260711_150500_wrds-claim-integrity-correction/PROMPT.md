# Objective

Correct the five-date real OptionMetrics run without changing its frozen input
panel: remove false Heston hedge attribution, persist calibration convergence
and bound diagnostics, fail closed on promotion eligibility, and add a genuine
analytic-Heston-delta hedge comparison against the market-IV Black-Scholes
delta baseline. Preserve restricted rows outside Git.
